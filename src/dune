(library
  (name optitrust)
  (public_name optitrust)
  (preprocess (pps ppx_transfo))
  (libraries compcert_parser clangml refl pprint str base64)
  (modes byte native))

(include_subdirs unqualified)

(env
  (dev
    (flags (:standard -warn-error -69 -w -9-27-32-33))))
