// #include "../../include/optitrust.h"
#include <stdio.h>
// #include <vector>

int main() {
  std::vector<int> x(10);
  bool b = x.size() > x.size();
  if (b) {

  }
  int s = x.size();
  if (s < s) {

  }
  /*if (((int)x.size()) < ((int)x.size())) {

  }
  */
}