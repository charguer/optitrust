# Usage: make transfo   or   make check
# Example usage for rebuilding just one test: make optitrust && make sequence_sub_out.cpp

# List of files to consider.
TESTS := \
  instruction_remove.ml \
  label_add.ml \
  label_remove.ml \
  loop_color.ml \
  loop_swap.ml \
  loop_tile.ml \
  loop_fusion.ml \
  loop_hoist.ml \
  loop_split \
  sequence_sub.ml \
  sequence_inline.ml \
  sequence_wrap.ml \
  sequence_unwrap.ml \
  sequence_sub.ml \
  sequence_insert.ml \
  struct_reorder.ml \
  array_tile.ml
  # \   array_to_variables.ml \
  # struct_set_implicit.ml \
# "struct_set_explicit.ml \
# const_insert.ml \
# declaration_remove.ml \

# To consider all ml files except specific ones, use EXCLUDE_TESTS (see ../Common.Makefile)

include ../Common.Makefile
