int main(int argc, char const *argv[])
{
  double a, b, c, d, e;
  double x;
  
  x = 3 * a + 2*b - b + c - 4 * a ;
  x = (a / b * c / (a * d * d / b * e));
  x = (a * b * c / a);
  x = ((a / (b / c)) + d * c / b) * (b / c);
    

  return 0;
}
