int main() {
  int x = 0;
  const int st = 0;
  const int N = 10;
  x += 0;
  for (int i = i + 1; i < 10; i++) {
    x += i;
  }
  x += st + 0;
  for (int j = j + 1; j < N; j++) {
    x += j;
  }
  int cut = 5;
  x += 0;
  for (int k = k + 1; k < N; k++) {
    x += k;
  }
  x += st + 0;
  for (int l = l + 1; l < N; l++) {
    x += l;
  }
}
