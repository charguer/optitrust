#include <stdlib.h>
#include "../../../include/optitrust.h"

int main () {
  const int N = 10;
  int* p = (int*) malloc (N * sizeof(int));

  int x;

y_seq: {
    int y;
    y = 0;
  }

  return 0;
}
