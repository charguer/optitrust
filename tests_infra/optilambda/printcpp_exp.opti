-- CPP test --
{
  record vect {
    x: int;
    y: int
  };
  record particle {
    pos: vect;
    speed: vect
  };
  record vects {
    head: vect;
    tail: ptr(vects)
  };
  type vect2 = vect;
  type vect3 = vect2;
  type int2 = array(int, 2);
  type intstar = ptr(int);
  fun addr_array_cell(): unit {
    letmut p;
    letmut n = p[0]
  };
  fun initlist(): unit {
    letmut v1 = record(1, 2);
    letmut v2 = record(1, 2);
    letmut p = record(1, 2);
    letmut n = (p)[0]
  };
  fun f(n: int): int {
    let __res: int = n;
    return __res
  };
  fun test_loop(): unit {
    letmut a = 0;
    for<seq, up>(i = 0, 10, 1) { __ignore(post_incr(a)) };
    for<seq, down_eq>(i = 10, 0, 1) { __ignore(post_decr(a)) };
    let x: int = 3;
    let y: int = 2;
    for_c(letmut x = 2, true, unit) {
      letmut y = 1;
      letmut r = x + y
    };
    let z: int = x + y
  };
  fun stack_var(): unit {
    letmut r = 3;
    r = r + 1 + 2;
    +=(r, 2);
    __ignore(post_incr(r));
    letmut s = f(r)
  };
  fun stack_array(): unit {
    letmut t = array(5, 6);
    letmut a = t[0];
    t[1] = a + 2
  };
  fun stack_struct(): unit {
    letmut v = record(5, 6);
    letmut a = v->x;
    v->y = a + 2;
    letmut v2 = v;
    v2 = v;
    letmut p1 = record(v, v);
    letmut p2 = record(v, record(7, 8))
  };
  fun references(): unit {
    letmut a = 3;
    let b: ptr(int) = a;
    b = b + 4
  };
  fun constants(): unit {
    let a: int = 3;
    let b: int = a + 3;
    letmut c = b + 4;
    let v: vect = record(0, 1);
    letmut d = v.x
  };
  fun const_pointers(): unit {
    letmut a = 3;
    letmut b = a;
    let c: int = b + 4
  };
  fun nonconst_pointers(): unit {
    letmut a = 3;
    letmut b = a;
    b = b + 4;
    letmut c = 3;
    b = c
  };
  fun main(): int {};
  fun h(x: int): int {
    letmut y = x + 1;
    let __res: int = y;
    return __res
  };
  fun immutable_stack_ptr(): int {
    letmut x = 3;
    letmut y = f(x);
    letmut p = x;
    let q: ptr(int) = x;
    let r: ptr(int) = x;
    p = y;
    q = 5;
    let __res: int = p + q + r;
    return __res
  };
  fun immutable_stack_array(): int {
    letmut x = 3;
    letmut y = 4;
    letmut t = array(x, y);
    t[0] = 3;
    letmut u = array(x, y);
    u[0] = y;
    letmut v = array(x, y);
    let __res: int = t[0];
    return __res
  };
  fun immutable_stack_var(): int {
    let a: int = 4;
    let r: int = 3;
    let s: int = r + 1;
    return r
  };
  fun mutable_stack_var(): int {
    letmut r = 3;
    r = r + 1;
    __ignore(post_incr(r));
    let __res: int = r;
    return __res
  };
  fun mutable_stack_array(): int {
    letmut x = 3;
    letmut y = 4;
    letmut w = array(x, y);
    w[0] = y;
    w[0] = 3;
    let __res: int = w[0];
    return __res
  };
  fun access_encoding(): unit {
    let a: vect = record(0, 1);
    let b: vect = a;
    letmut c = a;
    let ax: int = a.x;
    let cy: int = c->y
  };
  fun foo(v: vect): int {
    let __res: int = v.x;
    return __res
  };
  fun mutable_var_encoding(): int {
    let a: vect = record(0, 1);
    letmut ax = foo(a);
    letmut c = a;
    letmut cx = foo(c);
    let __res: int = cx;
    return __res
  };
  record vectpair {
    fst: vect;
    snd: vect
  };
  fun lvalue_encoding(): unit {
    letmut p;
    (p)->x = 2;
    (p)->x = 3;
    letmut q;
    ((q)->fst)->x = 2;
    ((q)->fst)->x = 3;
    letmut v;
    v = 4
  };
  fun arrow(): unit {
    letmut v = record(0, 1);
    letmut p = v;
    (p)->x = (p)->y;
    (p)->x = (p)->y
  };
  {
    fun __ghost_begin(#2: __ghost_fn, #3: __ghost_args, #4: __ghost_bind): __ghost_fn {
      return __admitted
    };
    fun __ghost_end(#5: __ghost_fn): unit {};
    fun __with_reverse(g: __ghost_fn, g_rev: __ghost_fn): __ghost_fn {
      let __res: __ghost_fn = g;
      return __res
    };
    fun __reverts(#6: __ghost_fn): unit {};
    fun __clear(#7: __ghost_args): __ghost_ret {};
    fun assert_inhabited(): __ghost_ret [T, x, x] {
      requires T: Type;
      requires x: T;
      ensures x: T
    };
    fun define(): __ghost_ret {};
    fun assert_prop(): __ghost_ret [P, proof, proof] {
      requires P: Prop;
      requires proof: P;
      ensures proof: P
    };
    fun assert_eq(): __ghost_ret [x, y, eq] {
      requires x: int;
      requires y: int;
      requires eq: __is_true(x = y)
    };
    fun assert_alias(): __ghost_ret {};
    fun rewrite_prop(): __ghost_ret [from, to, inside, by, #1, out] {
      requires from: int;
      requires to: int;
      requires inside: pure_fun(fun(#2: int): Type { Prop });
      requires by: __is_true(from = to);
      requires #1: inside(from);
      ensures out: inside(to);
      __admitted()
    };
    fun rewrite_linear(): __ghost_ret [from, to, inside, by, #4, #3] {
      requires from: int;
      requires to: int;
      requires inside: pure_fun(fun(#5: int): Type { HProp });
      requires by: __is_true(from = to);
      consumes #4: inside(from);
      produces #3: inside(to);
      __admitted()
    };
    fun rewrite_float_prop(): __ghost_ret [from, to, inside, by, #6, out] {
      requires from: f32;
      requires to: f32;
      requires inside: pure_fun(fun(#7: f32): Type { Prop });
      requires by: __is_true(from = to);
      requires #6: inside(from);
      ensures out: inside(to);
      __admitted()
    };
    fun rewrite_float_linear(): __ghost_ret [from, to, inside, by, #9, #8] {
      requires from: f32;
      requires to: f32;
      requires inside: pure_fun(fun(#10: f32): Type { HProp });
      requires by: __is_true(from = to);
      consumes #9: inside(from);
      produces #8: inside(to);
      __admitted()
    };
    fun rewrite_float_linear_admitted(): __ghost_ret [from, to, inside, #12, #11] {
      requires from: f32;
      requires to: f32;
      requires inside: pure_fun(fun(#13: f32): Type { HProp });
      consumes #12: inside(from);
      produces #11: inside(to);
      __admitted()
    };
    fun eq_refl_float(): __ghost_ret [x, out] {
      requires x: f32;
      ensures out: __is_true(x = x);
      __admitted()
    };
    fun eq_sym_float(): __ghost_ret [x, y, H, out] {
      requires x: f32;
      requires y: f32;
      requires H: __is_true(x = y);
      ensures out: __is_true(y = x);
      __admitted()
    };
    fun assume(): __ghost_ret [P, H] {
      requires P: Prop;
      ensures H: P;
      __admitted()
    };
    fun to_prove(): __ghost_ret [P, H] {
      requires P: Prop;
      ensures H: P;
      __admitted()
    };
    fun to_prove_hprop(): __ghost_ret [H1, H2, #15, #14] {
      requires H1: HProp;
      requires H2: HProp;
      consumes #15: H1;
      produces #14: H2;
      __admitted()
    };
    ghost(assert_prop()[proof := admit(pure_fun(fun(n: int, d: int): Type {
      __is_true(n - d + d = n)
    }))][z_cancel_minus_plus : proof]);
    ghost(assert_prop()[proof := admit(pure_fun(fun(n: int, d: int): Type {
      __is_true(n + d - d = n)
    }))][z_cancel_plus_minus : proof]);
    fun wrap_z_cancel_minus_plus(m: int): int [n, d, #17, #16] {
      requires n: int;
      requires d: int;
      requires #17: __is_true(m = n - d + d);
      ensures #16: __is_true(_Res = n);
      __admitted();
      return m;
      ghost(rewrite_linear()[inside := fun(v) { __is_true(_Res = v) }, by := z_cancel_minus_plus])
    };
    ghost(assert_prop()[proof := admit(pure_fun(fun(n: f32, d: f32): Type {
      __is_true(n - d + d = n)
    }))][r_cancel_minus_plus : proof]);
    ghost(assert_prop()[proof := admit(pure_fun(fun(n: f32, d: f32): Type {
      __is_true(n + d - d = n)
    }))][r_cancel_plus_minus : proof]);
    fun MINDEX0(): int {
      let __res: int = 0;
      return __res
    };
    fun MINDEX1(N1: int, i1: int): int {
      let __res: int = i1;
      return __res
    };
    fun MINDEX2(N1: int, N2: int, i1: int, i2: int): int {
      let __res: int = i1 * N2 + i2;
      return __res
    };
    fun MINDEX3(N1: int, N2: int, N3: int, i1: int, i2: int, i3: int): int {
      let __res: int = i1 * N2 * N3 + i2 * N3 + i3;
      return __res
    };
    fun MINDEX4(N1: int, N2: int, N3: int, N4: int, i1: int, i2: int, i3: int, i4: int): int {
      let __res: int = i1 * N2 * N3 * N4 + i2 * N3 * N4 + i3 * N4 + i4;
      return __res
    };
    fun MINDEX5(N1: int, N2: int, N3: int, N4: int, N5: int, i1: int, i2: int, i3: int, i4: int, i5: int): int {
      let __res: int = i1 * N2 * N3 * N4 * N5 + i2 * N3 * N4 * N5 + i3 * N4 * N5 + i4 * N5 + i5;
      return __res
    };
    fun DMINDEX0(): int {
      let __res: int = 0;
      return __res
    };
    fun DMINDEX1(N1: int, i1: int): int {
      let __res: int = 0;
      return __res
    };
    fun DMINDEX2(N1: int, N2: int, i1: int, i2: int): int {
      let __res: int = 0;
      return __res
    };
    fun DMINDEX3(N1: int, N2: int, N3: int, i1: int, i2: int, i3: int): int {
      let __res: int = 0;
      return __res
    };
    fun DMINDEX4(N1: int, N2: int, N3: int, N4: int, i1: int, i2: int, i3: int, i4: int): int {
      let __res: int = 0;
      return __res
    };
    fun DMINDEX5(N1: int, N2: int, N3: int, N4: int, N5: int, i1: int, i2: int, i3: int, i4: int, i5: int): int {
      let __res: int = 0;
      return __res
    };
    fun MSIZE0(): usize {
      let __res: int = 1;
      return __res
    };
    fun MSIZE1(N1: int): usize {
      let __res: usize = cast<usize>(N1);
      return __res
    };
    fun MSIZE2(N1: int, N2: int): usize {
      let __res: usize = cast<usize>(N1) * cast<usize>(N2);
      return __res
    };
    fun MSIZE3(N1: int, N2: int, N3: int): usize {
      let __res: usize = cast<usize>(N1) * cast<usize>(N2) * cast<usize>(N3);
      return __res
    };
    fun MSIZE4(N1: int, N2: int, N3: int, N4: int): usize {
      let __res: usize = cast<usize>(N1) * cast<usize>(N2) * cast<usize>(N3) * cast<usize>(N4);
      return __res
    };
    fun MSIZE5(N1: int, N2: int, N3: int, N4: int, N5: int): usize {
      let __res: usize = cast<usize>(N1) * cast<usize>(N2) * cast<usize>(N3) * cast<usize>(N4) * cast<usize>(N5);
      return __res
    };
    fun exact_div(n: int, b: int): int {
      __admitted();
      let __res: int = n / b;
      return __res
    };
    fun min(a: int, b: int): int {
      __admitted();
      return if (a < b) { a } else { b }
    };
    fun max(a: int, b: int): int {
      __admitted();
      return if (a > b) { a } else { b }
    };
    fun maxf(a: f32, b: f32): f32 {
      __admitted();
      return if (a > b) { a } else { b }
    };
    fun ANY(maxValue: int): int {
      let __res: int = 0;
      return __res
    };
    ghost(assert_inhabited()[x := arbitrary(pure_fun(fun(#286: HProp, #287: HProp): Type {
      HProp
    }))][Wand : x]);
    fun close_wand(): __ghost_ret [H1, H2, #20, #19, #18] {
      requires H1: HProp;
      requires H2: HProp;
      consumes #20: Wand(H1, H2);
      consumes #19: H1;
      produces #18: H2;
      __admitted()
    };
    fun hide(): __ghost_ret [H, #23, H2, #22, #21] {
      requires H: HProp;
      consumes #23: H;
      ensures H2: HProp;
      produces #22: Wand(H2, H);
      produces #21: H2;
      __admitted()
    };
    fun hide_rev(): __ghost_ret {
      reverts hide;
      ghost(close_wand())
    };
    fun wand_simplify(): __ghost_ret [H1, H2, H3, #26, #25, #24] {
      requires H1: HProp;
      requires H2: HProp;
      requires H3: HProp;
      consumes #26: Wand(H1, H2);
      consumes #25: Wand(H2, H3);
      produces #24: Wand(H1, H3);
      __admitted()
    };
    fun assert_hprop(): __ghost_ret [H, #28, #27] {
      requires H: HProp;
      consumes #28: H;
      produces #27: H
    };
    fun forget_init(): __ghost_ret {};
    ghost(assert_inhabited()[x := arbitrary(pure_fun(fun(#288: int, #289: Range): Type {
      Prop
    }))][in_range : x]);
    ghost(assert_inhabited()[x := arbitrary(pure_fun(fun(#290: Range, #291: Range): Type {
      Prop
    }))][is_subrange : x]);
    ghost(assert_inhabited()[x := arbitrary(pure_fun(fun(#292: Range): Type {
      int
    }))][range_count : x]);
    fun in_range_extend(): __ghost_ret [x, r1, r2, #31, #30, #29] {
      requires x: int;
      requires r1: Range;
      requires r2: Range;
      requires #31: in_range(x, r1);
      requires #30: is_subrange(r1, r2);
      ensures #29: in_range(x, r2);
      __admitted()
    };
    fun in_range_shift(): __ghost_ret [x, k, a, b, s, #33, #32] {
      requires x: int;
      requires k: int;
      requires a: int;
      requires b: int;
      requires s: int;
      requires #33: in_range(x, range(a, b, s));
      ensures #32: in_range(x + k, range(a + k, b + k, s));
      __admitted()
    };
    fun in_range_shift_extend(): __ghost_ret [x, k, r, a, b, s, #36, #35, #34] {
      requires x: int;
      requires k: int;
      requires r: Range;
      requires a: int;
      requires b: int;
      requires s: int;
      requires #36: in_range(x, range(a, b, s));
      requires #35: is_subrange(range(a + k, b + k, s), r);
      ensures #34: in_range(x + k, r);
      __admitted();
      ghost(in_range_shift()[x := x, k := k, a := a, b := b, s := s]);
      ghost(in_range_extend()[x := x + k, r1 := range(a + k, b + k, s), r2 := r])
    };
    fun in_range_bounds(): __ghost_ret [x, a, b, s, #38, #37, lower_bound, upper_bound] {
      requires x: int;
      requires a: int;
      requires b: int;
      requires s: int;
      requires #38: in_range(x, range(a, b, s));
      requires #37: __is_true(s >= 0);
      ensures lower_bound: __is_true(x >= a);
      ensures upper_bound: __is_true(x < b);
      __admitted()
    };
    fun in_range_bounds_rev(): __ghost_ret [x, a, b, s, #40, #39, lower_bound, upper_bound] {
      requires x: int;
      requires a: int;
      requires b: int;
      requires s: int;
      requires #40: in_range(x, range(a, b, s));
      requires #39: __is_true(s < 0);
      ensures lower_bound: __is_true(x > b);
      ensures upper_bound: __is_true(x <= a);
      __admitted()
    };
    fun bounds_to_in_range(): __ghost_ret [x, a, b, lower_bound, upper_bound, range_check] {
      requires x: int;
      requires a: int;
      requires b: int;
      requires lower_bound: __is_true(x >= a);
      requires upper_bound: __is_true(x < b);
      ensures range_check: in_range(x, range(a, b, 1));
      __admitted()
    };
    fun expand_subrange(): __ghost_ret [a, b, c, s, lower, up_ineq, #41] {
      requires a: int;
      requires b: int;
      requires c: int;
      requires s: int;
      requires lower: __is_true(s >= 0);
      requires up_ineq: __is_true(b <= c);
      ensures #41: is_subrange(range(a, b, s), range(a, c, s));
      __admitted()
    };
    fun ro_split2(): __ghost_ret [f, H, #44, #43, #42] {
      requires f: _Fraction;
      requires H: HProp;
      consumes #44: _RO(f, H);
      produces #43: _RO(__frac_div(f, 2), H);
      produces #42: _RO(__frac_div(f, 2), H);
      __admitted()
    };
    fun ro_split3(): __ghost_ret [f, H, #48, #47, #46, #45] {
      requires f: _Fraction;
      requires H: HProp;
      consumes #48: _RO(f, H);
      produces #47: _RO(__frac_div(f, 3), H);
      produces #46: _RO(__frac_div(f, 3), H);
      produces #45: _RO(__frac_div(f, 3), H);
      __admitted()
    };
    fun ro_split4(): __ghost_ret [f, H, #53, #52, #51, #50, #49] {
      requires f: _Fraction;
      requires H: HProp;
      consumes #53: _RO(f, H);
      produces #52: _RO(__frac_div(f, 4), H);
      produces #51: _RO(__frac_div(f, 4), H);
      produces #50: _RO(__frac_div(f, 4), H);
      produces #49: _RO(__frac_div(f, 4), H);
      __admitted()
    };
    fun ro_allow_join2(): __ghost_ret [f, H, #55, #54] {
      requires f: _Fraction;
      requires H: HProp;
      consumes #55: _RO(__frac_div(f, 2), H);
      produces #54: _RO(__frac_sub(f, __frac_div(f, 2)), H);
      __admitted()
    };
    fun ro_allow_join3(): __ghost_ret [f, H, #57, #56] {
      requires f: _Fraction;
      requires H: HProp;
      consumes #57: _RO(__frac_div(f, 3), H);
      produces #56: _RO(__frac_sub(__frac_sub(f, __frac_div(f, 3)), __frac_div(f, 3)), H);
      __admitted()
    };
    fun ro_allow_join4(): __ghost_ret [f, H, #59, #58] {
      requires f: _Fraction;
      requires H: HProp;
      consumes #59: _RO(__frac_div(f, 4), H);
      produces #58: _RO(__frac_sub(__frac_sub(__frac_sub(f, __frac_div(f, 4)), __frac_div(f, 4)), __frac_div(f, 4)), H);
      __admitted()
    };
    fun ro_fork_group(): __ghost_ret [f, H, r, #62, #61] {
      requires f: _Fraction;
      requires H: HProp;
      requires r: Range;
      consumes #62: _RO(f, H);
      produces #61: _RO(__frac_div(f, range_count(r)), Group(r, fun(#60: int) {
        H
      }));
      __admitted()
    };
    fun ro_join_group(): __ghost_ret {
      reverts ro_fork_group;
      __admitted()
    };
    fun swap_groups(): __ghost_ret [items, inner_range, outer_range, #64, #63] {
      requires items: pure_fun(fun(#65: int, #66: int): Type { HProp });
      requires inner_range: Range;
      requires outer_range: Range;
      consumes #64: Group(outer_range, fun(i: int) {
        Group(inner_range, fun(j: int) { items(i, j) })
      });
      produces #63: Group(inner_range, fun(j: int) {
        Group(outer_range, fun(i: int) { items(i, j) })
      });
      __admitted()
    };
    fun swap_groups_rev(): __ghost_ret {
      reverts swap_groups;
      __admitted()
    };
    fun ro_swap_groups(): __ghost_ret [items, inner_range, outer_range, f, #68, #67] {
      requires items: pure_fun(fun(#69: int, #70: int): Type { HProp });
      requires inner_range: Range;
      requires outer_range: Range;
      requires f: _Fraction;
      consumes #68: _RO(f, Group(outer_range, fun(i: int) {
        Group(inner_range, fun(j: int) { items(i, j) })
      }));
      produces #67: _RO(f, Group(inner_range, fun(j: int) {
        Group(outer_range, fun(i: int) { items(i, j) })
      }));
      __admitted()
    };
    fun ro_swap_groups_rev(): __ghost_ret {
      reverts ro_swap_groups;
      __admitted()
    };
    fun tiled_index_in_range(): __ghost_ret [tile_index, index, tile_count, tile_size, size, div_check, #73, #72, #71] {
      requires tile_index: int;
      requires index: int;
      requires tile_count: int;
      requires tile_size: int;
      requires size: int;
      requires div_check: __is_true(size = tile_count * tile_size);
      requires #73: in_range(tile_index, range(0, tile_count, 1));
      requires #72: in_range(index, range(0, tile_size, 1));
      ensures #71: in_range(tile_index * tile_size + index, range(0, size, 1));
      __admitted()
    };
    ghost(assert_prop()[proof := admit(pure_fun(fun(n: int): Type {
      __is_true(n = n)
    }))][eq_refl : proof]);
    ghost(assert_prop()[proof := admit(pure_fun(fun(m: int, n: int, eq: __is_true(m = n)): Type {
      __is_true(n = m)
    }))][eq_sym : proof]);
    ghost(assert_prop()[proof := admit(pure_fun(fun(n: int): Type {
      __is_true(0 = 0 * n)
    }))][zero_mul_intro : proof]);
    ghost(assert_prop()[proof := admit(pure_fun(fun(n: int): Type {
      __is_true(n = n + 0)
    }))][plus_zero_intro : proof]);
    ghost(assert_prop()[proof := admit(pure_fun(fun(m: int, n: int, p: int): Type {
      __is_true(m + n + p = m + (n + p))
    }))][add_assoc_right : proof]);
    ghost(assert_prop()[proof := admit(pure_fun(fun(m: int, n: int): Type {
      __is_true(m * n + n = (m + 1) * n)
    }))][mul_add_factor : proof]);
    fun tile_divides(): __ghost_ret [tile_count, tile_size, size, items, div_check, positive_tile_size, #75, #74] {
      requires tile_count: int;
      requires tile_size: int;
      requires size: int;
      requires items: pure_fun(fun(#76: int): Type { HProp });
      requires div_check: __is_true(size = tile_count * tile_size);
      requires positive_tile_size: __is_true(tile_size >= 0);
      consumes #75: Group(range(0, size, 1), items);
      produces #74: Group(range(0, tile_count, 1), fun(bi: int) {
        Group(range(0, tile_size, 1), fun(i: int) { items(bi * tile_size + i) })
      });
      __admitted()
    };
    fun untile_divides(): __ghost_ret {
      reverts tile_divides;
      __admitted()
    };
    fun ro_tile_divides(): __ghost_ret [tile_count, tile_size, size, items, div_check, positive_tile_size, f, #78, #77] {
      requires tile_count: int;
      requires tile_size: int;
      requires size: int;
      requires items: pure_fun(fun(#79: int): Type { HProp });
      requires div_check: __is_true(size = tile_count * tile_size);
      requires positive_tile_size: __is_true(tile_size >= 0);
      requires f: _Fraction;
      consumes #78: _RO(f, Group(range(0, size, 1), items));
      produces #77: _RO(f, Group(range(0, tile_count, 1), fun(bi: int) {
        Group(range(0, tile_size, 1), fun(i: int) { items(bi * tile_size + i) })
      }));
      __admitted()
    };
    fun ro_untile_divides(): __ghost_ret {
      reverts ro_tile_divides;
      __admitted()
    };
    fun group_collapse(): __ghost_ret [n, m, items, #81, #80] {
      requires n: int;
      requires m: int;
      requires items: pure_fun(fun(#82: int, #83: int): Type { HProp });
      consumes #81: Group(range(0, n, 1), fun(i: int) {
        Group(range(0, m, 1), fun(j: int) { items(i, j) })
      });
      produces #80: Group(range(0, n * m, 1), fun(ij: int) {
        items(ij / m, ij % m)
      });
      __admitted()
    };
    fun group_uncollapse(): __ghost_ret {
      reverts group_collapse;
      __admitted()
    };
    fun ro_group_collapse(): __ghost_ret [n, m, items, f, #85, #84] {
      requires n: int;
      requires m: int;
      requires items: pure_fun(fun(#86: int, #87: int): Type { HProp });
      requires f: _Fraction;
      consumes #85: _RO(f, Group(range(0, n, 1), fun(i: int) {
        Group(range(0, m, 1), fun(j: int) { items(i, j) })
      }));
      produces #84: _RO(f, Group(range(0, n * m, 1), fun(ij: int) {
        items(ij / m, ij % m)
      }));
      __admitted()
    };
    fun ro_group_uncollapse(): __ghost_ret {
      reverts ro_group_collapse;
      __admitted()
    };
    fun group_focus(): __ghost_ret [i, range, items, bound_check, #90, #89, #88] {
      requires i: int;
      requires range: Range;
      requires items: pure_fun(fun(#91: int): Type { HProp });
      requires bound_check: in_range(i, range);
      consumes #90: Group(range, items);
      produces #89: Wand(items(i), Group(range, items));
      produces #88: items(i);
      __admitted()
    };
    fun group_unfocus(): __ghost_ret {
      reverts group_focus;
      __admitted();
      ghost(close_wand())
    };
    fun ro_group_focus(): __ghost_ret [i, range, items, f, bound_check, #94, #93, #92] {
      requires i: int;
      requires range: Range;
      requires items: pure_fun(fun(#95: int): Type { HProp });
      requires f: _Fraction;
      requires bound_check: in_range(i, range);
      consumes #94: _RO(f, Group(range, items));
      produces #93: Wand(_RO(f, items(i)), _RO(f, Group(range, items)));
      produces #92: _RO(f, items(i));
      __admitted()
    };
    fun ro_group_unfocus(): __ghost_ret {
      reverts ro_group_focus;
      __admitted();
      ghost(close_wand())
    };
    fun ro_group2_focus(): __ghost_ret [i, r, r2, items, f, bound_check, #98, #97, #96] {
      requires i: int;
      requires r: Range;
      requires r2: Range;
      requires items: pure_fun(fun(#99: int, #100: int): Type { HProp });
      requires f: _Fraction;
      requires bound_check: in_range(i, r);
      consumes #98: _RO(f, Group(r2, fun(i2: int) {
        Group(r, fun(i: int) { items(i2, i) })
      }));
      produces #97: Wand(_RO(f, Group(r2, fun(i2: int) { items(i2, i) })), _RO(f, Group(r2, fun(i2: int) {
        Group(r, fun(i: int) { items(i2, i) })
      })));
      produces #96: _RO(f, Group(r2, fun(i2: int) { items(i2, i) }));
      __admitted()
    };
    fun ro_group2_unfocus(): __ghost_ret {
      reverts ro_group2_focus;
      ghost(close_wand())
    };
    fun group_focus_subrange(): __ghost_ret [sub_range, big_range, items, bound_check, #103, #102, #101] {
      requires sub_range: Range;
      requires big_range: Range;
      requires items: pure_fun(fun(#104: int): Type { HProp });
      requires bound_check: is_subrange(sub_range, big_range);
      consumes #103: Group(big_range, items);
      produces #102: Wand(Group(sub_range, items), Group(big_range, items));
      produces #101: Group(sub_range, items);
      __admitted()
    };
    fun group_unfocus_subrange(): __ghost_ret {
      reverts group_focus_subrange;
      ghost(close_wand())
    };
    fun ro_group_focus_subrange(): __ghost_ret [sub_range, big_range, items, f, bound_check, #107, #106, #105] {
      requires sub_range: Range;
      requires big_range: Range;
      requires items: pure_fun(fun(#108: int): Type { HProp });
      requires f: _Fraction;
      requires bound_check: is_subrange(sub_range, big_range);
      consumes #107: _RO(f, Group(big_range, items));
      produces #106: Wand(_RO(f, Group(sub_range, items)), _RO(f, Group(big_range, items)));
      produces #105: _RO(f, Group(sub_range, items));
      __admitted()
    };
    fun ro_group_unfocus_subrange(): __ghost_ret {
      reverts ro_group_focus_subrange;
      ghost(close_wand())
    };
    fun group2_focus_subrange(): __ghost_ret [outer_range, sub_range, big_range, items, bound_check, #111, #110, #109] {
      requires outer_range: Range;
      requires sub_range: Range;
      requires big_range: Range;
      requires items: pure_fun(fun(#113: int): Type {
        pure_fun(fun(#112: int): Type { HProp })
      });
      requires bound_check: is_subrange(sub_range, big_range);
      consumes #111: Group(outer_range, fun(i: int) {
        Group(big_range, items(i))
      });
      produces #110: Wand(Group(outer_range, fun(i: int) {
        Group(sub_range, items(i))
      }), Group(outer_range, fun(i: int) { Group(big_range, items(i)) }));
      produces #109: Group(outer_range, fun(i: int) {
        Group(sub_range, items(i))
      });
      __admitted()
    };
    fun group2_unfocus_subrange(): __ghost_ret {
      reverts group2_focus_subrange;
      __admitted()
    };
    fun group_shift(): __ghost_ret [start, stop, step, items, shift, new_start, new_stop, check_start, check_stop, #115, #114] {
      requires start: int;
      requires stop: int;
      requires step: int;
      requires items: pure_fun(fun(#116: int): Type { HProp });
      requires shift: int;
      requires new_start: int;
      requires new_stop: int;
      requires check_start: __is_true(new_start = start + shift);
      requires check_stop: __is_true(new_stop = stop + shift);
      consumes #115: Group(range(start, stop, step), fun(i: int) { items(i) });
      produces #114: Group(range(new_start, new_stop, step), fun(i: int) {
        items(i - shift)
      });
      __admitted()
    };
    fun group_unshift(): __ghost_ret {
      reverts group_shift;
      __admitted()
    };
    fun ro_group_shift(): __ghost_ret [start, stop, step, items, shift, new_start, new_stop, check_start, check_stop, f, #118, #117] {
      requires start: int;
      requires stop: int;
      requires step: int;
      requires items: pure_fun(fun(#119: int): Type { HProp });
      requires shift: int;
      requires new_start: int;
      requires new_stop: int;
      requires check_start: __is_true(new_start = start + shift);
      requires check_stop: __is_true(new_stop = stop + shift);
      requires f: _Fraction;
      consumes #118: _RO(f, Group(range(start, stop, step), fun(i: int) {
        items(i)
      }));
      produces #117: _RO(f, Group(range(new_start, new_stop, step), fun(i: int) {
        items(i - shift)
      }));
      __admitted()
    };
    fun ro_group_unshift(): __ghost_ret {
      reverts ro_group_shift;
      __admitted()
    };
    fun group_scale(): __ghost_ret [stop, step, items, factor, new_step, new_stop, check_stop, check_step, check_factor, #121, #120] {
      requires stop: int;
      requires step: int;
      requires items: pure_fun(fun(#122: int): Type { HProp });
      requires factor: int;
      requires new_step: int;
      requires new_stop: int;
      requires check_stop: __is_true(new_stop = factor * stop);
      requires check_step: __is_true(new_step = factor * step);
      requires check_factor: __is_true(factor <> 0);
      consumes #121: Group(range(0, stop, step), fun(i: int) { items(i) });
      produces #120: Group(range(0, new_stop, new_step), fun(i: int) {
        items(i / factor)
      });
      __admitted()
    };
    fun group_unscale(): __ghost_ret {
      reverts group_scale;
      __admitted()
    };
    fun ro_group_scale(): __ghost_ret [stop, step, items, factor, new_step, new_stop, check_stop, check_step, f, #124, #123] {
      requires stop: int;
      requires step: int;
      requires items: pure_fun(fun(#125: int): Type { HProp });
      requires factor: int;
      requires new_step: int;
      requires new_stop: int;
      requires check_stop: __is_true(new_stop = factor * stop);
      requires check_step: __is_true(new_step = factor * step);
      requires f: _Fraction;
      consumes #124: _RO(f, Group(range(0, stop, step), fun(i: int) {
        items(i)
      }));
      produces #123: _RO(f, Group(range(0, new_stop, new_step), fun(i: int) {
        items(i / factor)
      }));
      __admitted()
    };
    fun ro_group_unscale(): __ghost_ret {
      reverts ro_group_scale;
      __admitted()
    };
    fun group_split(): __ghost_ret [start, stop, step, split, items, range_check, #128, #127, #126] {
      requires start: int;
      requires stop: int;
      requires step: int;
      requires split: int;
      requires items: pure_fun(fun(#129: int): Type { HProp });
      requires range_check: is_subrange(range(start, split, step), range(start, stop, step));
      consumes #128: Group(range(start, stop, step), fun(i: int) { items(i) });
      produces #127: Group(range(start, split, step), fun(i: int) { items(i) });
      produces #126: Group(range(split, stop, step), fun(i: int) { items(i) });
      __admitted()
    };
    fun group_join(): __ghost_ret {
      reverts group_split;
      __admitted()
    };
    fun ro_group_split(): __ghost_ret [start, stop, step, split, items, bound_check, f, #132, #131, #130] {
      requires start: int;
      requires stop: int;
      requires step: int;
      requires split: int;
      requires items: pure_fun(fun(#133: int): Type { HProp });
      requires bound_check: in_range(split, range(start, stop, step));
      requires f: _Fraction;
      consumes #132: _RO(f, Group(range(start, stop, step), fun(i: int) {
        items(i)
      }));
      produces #131: _RO(f, Group(range(start, split, step), fun(i: int) {
        items(i)
      }));
      produces #130: _RO(f, Group(range(split, stop, step), fun(i: int) {
        items(i)
      }));
      __admitted()
    };
    fun ro_group_join(): __ghost_ret {
      reverts ro_group_split;
      __admitted()
    };
    fun pure_group_split(): __ghost_ret [start, stop, step, split, items, bound_check, #139, #137, #135] {
      requires start: int;
      requires stop: int;
      requires step: int;
      requires split: int;
      requires items: pure_fun(fun(#140: int): Type { Prop });
      requires bound_check: in_range(split, range(start, stop, step));
      requires #139: pure_fun(fun(i: int, #138: in_range(i, range(start, stop, step))): Type {
        items(i)
      });
      ensures #137: pure_fun(fun(i: int, #136: in_range(i, range(start, split, step))): Type {
        items(i)
      });
      ensures #135: pure_fun(fun(i: int, #134: in_range(i, range(split, stop, step))): Type {
        items(i)
      });
      __admitted()
    };
    fun pure_group_join(): __ghost_ret {
      reverts pure_group_split;
      __admitted()
    };
    fun group_intro_zero(): __ghost_ret [items, #141] {
      requires items: pure_fun(fun(#142: int): Type { HProp });
      produces #141: Group(range(0, 0, 1), fun(i: int) { items(i) });
      __admitted()
    };
    fun group_intro_empty(): __ghost_ret [N, items, #143] {
      requires N: int;
      requires items: pure_fun(fun(#144: int): Type { HProp });
      produces #143: Group(range(N, N, 1), fun(i: int) { items(i) });
      __admitted()
    };
    fun group_elim_zero(): __ghost_ret {
      reverts group_intro_zero;
      __admitted()
    };
    fun group_elim_empty(): __ghost_ret {
      reverts group_intro_empty;
      __admitted()
    };
    fun group_intro_one(): __ghost_ret [item, #146, #145] {
      requires item: HProp;
      consumes #146: item;
      produces #145: Group(range(0, 1, 1), fun(i: int) { item });
      __admitted()
    };
    fun group_elim_one(): __ghost_ret {
      reverts group_intro_one;
      __admitted()
    };
    fun dmindex2_untile(): __ghost_ret [H, n1, n2, #148, #147] {
      requires H: pure_fun(fun(#151: pure_fun(fun(#149: int, #150: int): Type {
        int
      })): Type { HProp });
      requires n1: int;
      requires n2: int;
      consumes #148: H(fun(i1, i2) { DMINDEX1(n1 * n2, i1 * n2 + i2) });
      produces #147: H(fun(i1, i2) { DMINDEX2(n1, n2, i1, i2) });
      __admitted()
    };
    fun dmindex2_tile(): __ghost_ret {
      reverts dmindex2_untile;
      __admitted()
    };
    fun dmindex3_untile(): __ghost_ret [H, n1, n2, n3, #153, #152] {
      requires H: pure_fun(fun(#157: pure_fun(fun(#154: int, #155: int, #156: int): Type {
        int
      })): Type { HProp });
      requires n1: int;
      requires n2: int;
      requires n3: int;
      consumes #153: H(fun(i1, i2, i3) {
        DMINDEX2(n1 * n2, n3, i1 * n2 + i2, i3)
      });
      produces #152: H(fun(i1, i2, i3) { DMINDEX3(n1, n2, n3, i1, i2, i3) });
      __admitted()
    };
    fun dmindex3_tile(): __ghost_ret {
      reverts dmindex3_untile;
      __admitted()
    };
    fun mindex2_unfold(): __ghost_ret [T, H, matrix, n1, n2, #159, #158] {
      requires T: Type;
      requires H: pure_fun(fun(#162: pure_fun(fun(#160: int, #161: int): Type {
        ptr(T)
      })): Type { HProp });
      requires matrix: ptr(T);
      requires n1: int;
      requires n2: int;
      consumes #159: H(fun(i1, i2) { matrix[MINDEX2(n1, n2, i1, i2)] });
      produces #158: H(fun(i1, i2) { matrix[i1 * n2][MINDEX1(n2, i2)] });
      __admitted()
    };
    fun mindex2_fold(): __ghost_ret {
      reverts mindex2_unfold;
      __admitted()
    };
    fun mindex3_unfold(): __ghost_ret [T, H, matrix, n1, n2, n3, #164, #163] {
      requires T: Type;
      requires H: pure_fun(fun(#168: pure_fun(fun(#165: int, #166: int, #167: int): Type {
        ptr(T)
      })): Type { HProp });
      requires matrix: ptr(T);
      requires n1: int;
      requires n2: int;
      requires n3: int;
      consumes #164: H(fun(i1, i2, i3) {
        matrix[MINDEX3(n1, n2, n3, i1, i2, i3)]
      });
      produces #163: H(fun(i1, i2, i3) {
        matrix[i1 * n2 * n3][MINDEX2(n2, n3, i2, i3)]
      });
      __admitted()
    };
    fun mindex3_fold(): __ghost_ret {
      reverts mindex3_unfold;
      __admitted()
    };
    fun ro_mindex2_unfold(): __ghost_ret [T, H, matrix, n1, n2, f, #170, #169] {
      requires T: Type;
      requires H: pure_fun(fun(#173: pure_fun(fun(#171: int, #172: int): Type {
        ptr(T)
      })): Type { HProp });
      requires matrix: ptr(T);
      requires n1: int;
      requires n2: int;
      requires f: _Fraction;
      consumes #170: _RO(f, H(fun(i1, i2) { matrix[MINDEX2(n1, n2, i1, i2)] }));
      produces #169: _RO(f, H(fun(i1, i2) { matrix[i1 * n2][MINDEX1(n2, i2)] }));
      __admitted()
    };
    fun ro_mindex2_fold(): __ghost_ret {
      reverts ro_mindex2_unfold;
      __admitted()
    };
    fun ro_mindex3_unfold(): __ghost_ret [T, H, matrix, n1, n2, n3, f, #175, #174] {
      requires T: Type;
      requires H: pure_fun(fun(#179: pure_fun(fun(#176: int, #177: int, #178: int): Type {
        ptr(T)
      })): Type { HProp });
      requires matrix: ptr(T);
      requires n1: int;
      requires n2: int;
      requires n3: int;
      requires f: _Fraction;
      consumes #175: _RO(f, H(fun(i1, i2, i3) {
        matrix[MINDEX3(n1, n2, n3, i1, i2, i3)]
      }));
      produces #174: _RO(f, H(fun(i1, i2, i3) {
        matrix[i1 * n2 * n3][MINDEX2(n2, n3, i2, i3)]
      }));
      __admitted()
    };
    fun ro_mindex3_fold(): __ghost_ret {
      reverts ro_mindex3_unfold;
      __admitted()
    };
    ghost(assert_inhabited()[x := arbitrary(pure_fun(fun(#293: Prop, #294: HProp): Type {
      HProp
    }))][If : x]);
    fun if_false_hprop_rewrite(): __ghost_ret [b, H, H2, HP, #181, #180] {
      requires b: bool;
      requires H: HProp;
      requires H2: HProp;
      requires HP: __is_false(b);
      consumes #181: If(__is_true(b), H);
      produces #180: If(__is_true(b), H2);
      __admitted()
    };
    fun if_false_hprop_drop(): __ghost_ret [b, H, HP, #182] {
      requires b: bool;
      requires H: HProp;
      requires HP: __is_false(b);
      consumes #182: If(__is_true(b), H);
      __admitted()
    };
    fun if_true_hprop_elim(): __ghost_ret [b, H, HP, #184, #183] {
      requires b: bool;
      requires H: HProp;
      requires HP: __is_true(b);
      consumes #184: If(__is_true(b), H);
      produces #183: H;
      __admitted()
    };
    fun if_true_hprop_intro(): __ghost_ret [b, H, HP, #186, #185] {
      requires b: bool;
      requires H: HProp;
      requires HP: __is_true(b);
      consumes #186: H;
      produces #185: If(__is_true(b), H);
      __admitted()
    };
    fun group_expand_r_if_intros(): __ghost_ret [n1, n2, items, expand_check, #188, #187] {
      requires n1: int;
      requires n2: int;
      requires items: pure_fun(fun(#189: int): Type { HProp });
      requires expand_check: __is_true(n1 <= n2);
      consumes #188: Group(range(0, n1, 1), fun(i: int) { items(i) });
      produces #187: Group(range(0, n2, 1), fun(i: int) {
        If(__is_true(i < n1), items(i))
      });
      __admitted()
    };
    fun group_shrink_r_if_elim(): __ghost_ret {
      reverts group_expand_r_if_intros;
      __admitted()
    };
    fun group_singleton_if_intros(): __ghost_ret [n, H, #191, #190] {
      requires n: int;
      requires H: HProp;
      consumes #191: H;
      produces #190: Group(range(0, n, 1), fun(i: int) {
        If(__is_true(i = 0), H)
      });
      __admitted()
    };
    fun group_singleton_if_elim(): __ghost_ret [n, H, #193, #192] {
      requires n: int;
      requires H: HProp;
      consumes #193: Group(range(0, n, 1), fun(i: int) {
        If(__is_true(i = 0), H)
      });
      produces #192: H;
      __admitted()
    };
    ghost(assert_prop()[proof := admit(pure_fun(fun(b: int, e1: int, e2: int, #295: __is_true(e1 <= e2)): Type {
      __is_true(b << e1 <= b << e2)
    }))][shiftr_monotonic : proof]);
    let __rewrite_sequence: const(int);
    fun group_to_desyncgroup(): __ghost_ret [N, items, r, #196, #195, #196, #194] {
      requires N: int;
      requires items: pure_fun(fun(#197: int): Type { HProp });
      requires r: Range;
      consumes #196: ThreadsCtx(r);
      consumes #195: Group(range(0, N, 1), fun(i: int) { items(i) });
      produces #196: ThreadsCtx(r);
      produces #194: DesyncGroup(N, fun(i: int) { items(i) });
      __admitted()
    };
    fun unwrap_singleton_desyncgroup(): __ghost_ret [t, H, #200, #199, #200, #198] {
      requires t: int;
      requires H: pure_fun(fun(#201: int): Type { HProp });
      consumes #200: ThreadsCtx(counted_range(t, MSIZE0()));
      consumes #199: DesyncGroup(MSIZE0(), H);
      produces #200: ThreadsCtx(counted_range(t, MSIZE0()));
      produces #198: H(0);
      __admitted()
    };
    fun desync_tile_divides(): __ghost_ret [tile_count, tile_size, size, items, div_check, positive_tile_size, #203, #202] {
      requires tile_count: int;
      requires tile_size: int;
      requires size: int;
      requires items: pure_fun(fun(#204: int): Type { HProp });
      requires div_check: __is_true(size = tile_count * tile_size);
      requires positive_tile_size: __is_true(tile_size >= 0);
      consumes #203: DesyncGroup(size, items);
      produces #202: DesyncGroup(tile_count, fun(bi: int) {
        DesyncGroup(tile_size, fun(i: int) { items(bi * tile_size + i) })
      });
      __admitted()
    };
    fun desync_untile_divides(): __ghost_ret {
      reverts desync_tile_divides;
      __admitted()
    };
    fun singleton_mindex_simplify(): __ghost_ret [T, H, p, #206, #205] {
      requires T: Type;
      requires H: pure_fun(fun(#207: ptr(T)): Type { HProp });
      requires p: ptr(T);
      consumes #206: H(p[MINDEX1(MSIZE0(), DMINDEX1(MSIZE0(), 0))]);
      produces #205: H(p);
      __admitted()
    }
  };
  {
    let __OPTITRUST_ENABLE_MODELS: __ghost_unit;
    fun ro_matrix1_focus(): __ghost_ret [T, matrix, i, n, MT, M, f, bound_check, #210, #209, #208] {
      requires T: Type;
      requires matrix: ptr(T);
      requires i: int;
      requires n: int;
      requires MT: MemType;
      requires M: pure_fun(fun(#211: int): Type { T });
      requires f: _Fraction;
      requires bound_check: in_range(i, range(0, n, 1));
      consumes #210: _RO(f, Group(range(0, n, 1), fun(i1: int) {
        ~>(matrix[MINDEX1(n, i1)], CellOf(MT)(M(i1)))
      }));
      produces #209: Wand(_RO(f, ~>(matrix[MINDEX1(n, i)], CellOf(MT)(M(i)))), _RO(f, Group(range(0, n, 1), fun(i1: int) {
        ~>(matrix[MINDEX1(n, i1)], CellOf(MT)(M(i1)))
      })));
      produces #208: _RO(f, ~>(matrix[MINDEX1(n, i)], CellOf(MT)(M(i))));
      __admitted();
      ghost(ro_group_focus()[f := f, i := i, bound_check := bound_check])
    };
    fun ro_matrix1_unfocus(): __ghost_ret {
      reverts ro_matrix1_focus;
      __admitted();
      ghost(close_wand())
    };
    fun ro_matrix2_focus(): __ghost_ret [T, matrix, i, j, m, n, MT, M, f, bound_check_i, bound_check_j, #214, #213, #212] {
      requires T: Type;
      requires matrix: ptr(T);
      requires i: int;
      requires j: int;
      requires m: int;
      requires n: int;
      requires MT: MemType;
      requires M: pure_fun(fun(#215: int, #216: int): Type { T });
      requires f: _Fraction;
      requires bound_check_i: in_range(i, range(0, m, 1));
      requires bound_check_j: in_range(j, range(0, n, 1));
      consumes #214: _RO(f, Group(range(0, m, 1), fun(i1: int) {
        Group(range(0, n, 1), fun(i2: int) {
          ~>(matrix[MINDEX2(m, n, i1, i2)], CellOf(MT)(M(i1, i2)))
        })
      }));
      produces #213: Wand(_RO(f, ~>(matrix[MINDEX2(m, n, i, j)], CellOf(MT)(M(i, j)))), _RO(f, Group(range(0, m, 1), fun(i1: int) {
        Group(range(0, n, 1), fun(i2: int) {
          ~>(matrix[MINDEX2(m, n, i1, i2)], CellOf(MT)(M(i1, i2)))
        })
      })));
      produces #212: _RO(f, ~>(matrix[MINDEX2(m, n, i, j)], CellOf(MT)(M(i, j))));
      __admitted();
      ghost(ro_group_focus()[f := f, i := i, bound_check := bound_check_i]);
      ghost(ro_group_focus()[f := f, i := j, bound_check := bound_check_j]);
      ghost(wand_simplify())
    };
    fun ro_matrix2_unfocus(): __ghost_ret {
      reverts ro_matrix2_focus;
      __admitted();
      ghost(close_wand())
    };
    fun MATRIX1_COPY_int(dest: ptr(int), src: ptr(int), length: int): unit [model, #219, #218, #217, #218, #217] {
      requires model: pure_fun(fun(#220: int): Type { int });
      requires #219: _Fraction;
      consumes #218: _RO(#219, Group(range(0, length, 1), fun(i1: int) {
        ~>(src[MINDEX1(length, i1)], CellOf(Any)(model(i1)))
      }));
      consumes #217: Group(range(0, length, 1), fun(i1: int) {
        ~>(dest[MINDEX1(length, i1)], UninitCellOf(Any))
      });
      produces #218: _RO(#219, Group(range(0, length, 1), fun(i1: int) {
        ~>(src[MINDEX1(length, i1)], CellOf(Any)(model(i1)))
      }));
      produces #217: Group(range(0, length, 1), fun(i1: int) {
        ~>(dest[MINDEX1(length, i1)], CellOf(Any)(model(i1)))
      });
      __admitted();
      __ignore(memcpy(dest, src, length * sizeof(int)))
    };
    fun MATRIX2_COPY_int(dest: ptr(int), src: ptr(int), n1: int, n2: int): unit [model, #223, #222, #221, #222, #221] {
      requires model: pure_fun(fun(#224: int, #225: int): Type { int });
      requires #223: _Fraction;
      consumes #222: _RO(#223, Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          ~>(src[MINDEX2(n1, n2, i1, i2)], CellOf(Any)(model(i1, i2)))
        })
      }));
      consumes #221: Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          ~>(dest[MINDEX2(n1, n2, i1, i2)], UninitCellOf(Any))
        })
      });
      produces #222: _RO(#223, Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          ~>(src[MINDEX2(n1, n2, i1, i2)], CellOf(Any)(model(i1, i2)))
        })
      }));
      produces #221: Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          ~>(dest[MINDEX2(n1, n2, i1, i2)], CellOf(Any)(model(i1, i2)))
        })
      });
      __admitted();
      __ignore(memcpy(dest, src, n1 * n2 * sizeof(int)))
    };
    fun MATRIX3_COPY_int(dest: ptr(int), src: ptr(int), n1: int, n2: int, n3: int): unit [model, #228, #227, #226, #227, #226] {
      requires model: pure_fun(fun(#229: int, #230: int, #231: int): Type {
        int
      });
      requires #228: _Fraction;
      consumes #227: _RO(#228, Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(src[MINDEX3(n1, n2, n3, i1, i2, i3)], CellOf(Any)(model(i1, i2, i3)))
          })
        })
      }));
      consumes #226: Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(dest[MINDEX3(n1, n2, n3, i1, i2, i3)], UninitCellOf(Any))
          })
        })
      });
      produces #227: _RO(#228, Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(src[MINDEX3(n1, n2, n3, i1, i2, i3)], CellOf(Any)(model(i1, i2, i3)))
          })
        })
      }));
      produces #226: Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(dest[MINDEX3(n1, n2, n3, i1, i2, i3)], CellOf(Any)(model(i1, i2, i3)))
          })
        })
      });
      __admitted();
      __ignore(memcpy(dest, src, n1 * n2 * n3 * sizeof(int)))
    };
    fun MATRIX1_COPY_float(dest: ptr(f32), src: ptr(f32), length: int): unit [model, #234, #233, #232, #233, #232] {
      requires model: pure_fun(fun(#235: int): Type { f32 });
      requires #234: _Fraction;
      consumes #233: _RO(#234, Group(range(0, length, 1), fun(i1: int) {
        ~>(src[MINDEX1(length, i1)], CellOf(Any)(model(i1)))
      }));
      consumes #232: Group(range(0, length, 1), fun(i1: int) {
        ~>(dest[MINDEX1(length, i1)], UninitCellOf(Any))
      });
      produces #233: _RO(#234, Group(range(0, length, 1), fun(i1: int) {
        ~>(src[MINDEX1(length, i1)], CellOf(Any)(model(i1)))
      }));
      produces #232: Group(range(0, length, 1), fun(i1: int) {
        ~>(dest[MINDEX1(length, i1)], CellOf(Any)(model(i1)))
      });
      __admitted();
      __ignore(memcpy(dest, src, length * sizeof(f32)))
    };
    fun MATRIX2_COPY_float(dest: ptr(f32), src: ptr(f32), n1: int, n2: int): unit [model, #238, #237, #236, #237, #236] {
      requires model: pure_fun(fun(#239: int, #240: int): Type { f32 });
      requires #238: _Fraction;
      consumes #237: _RO(#238, Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          ~>(src[MINDEX2(n1, n2, i1, i2)], CellOf(Any)(model(i1, i2)))
        })
      }));
      consumes #236: Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          ~>(dest[MINDEX2(n1, n2, i1, i2)], UninitCellOf(Any))
        })
      });
      produces #237: _RO(#238, Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          ~>(src[MINDEX2(n1, n2, i1, i2)], CellOf(Any)(model(i1, i2)))
        })
      }));
      produces #236: Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          ~>(dest[MINDEX2(n1, n2, i1, i2)], CellOf(Any)(model(i1, i2)))
        })
      });
      __admitted();
      __ignore(memcpy(dest, src, n1 * n2 * sizeof(f32)))
    };
    fun MATRIX3_COPY_float(dest: ptr(f32), src: ptr(f32), n1: int, n2: int, n3: int): unit [model, #243, #242, #241, #242, #241] {
      requires model: pure_fun(fun(#244: int, #245: int, #246: int): Type {
        f32
      });
      requires #243: _Fraction;
      consumes #242: _RO(#243, Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(src[MINDEX3(n1, n2, n3, i1, i2, i3)], CellOf(Any)(model(i1, i2, i3)))
          })
        })
      }));
      consumes #241: Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(dest[MINDEX3(n1, n2, n3, i1, i2, i3)], UninitCellOf(Any))
          })
        })
      });
      produces #242: _RO(#243, Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(src[MINDEX3(n1, n2, n3, i1, i2, i3)], CellOf(Any)(model(i1, i2, i3)))
          })
        })
      }));
      produces #241: Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(dest[MINDEX3(n1, n2, n3, i1, i2, i3)], CellOf(Any)(model(i1, i2, i3)))
          })
        })
      });
      __admitted();
      __ignore(memcpy(dest, src, n1 * n2 * n3 * sizeof(f32)))
    };
    fun MATRIX1_COPY_double(dest: ptr(f64), src: ptr(f64), length: int): unit [model, #249, #248, #247, #248, #247] {
      requires model: pure_fun(fun(#250: int): Type { f64 });
      requires #249: _Fraction;
      consumes #248: _RO(#249, Group(range(0, length, 1), fun(i1: int) {
        ~>(src[MINDEX1(length, i1)], CellOf(Any)(model(i1)))
      }));
      consumes #247: Group(range(0, length, 1), fun(i1: int) {
        ~>(dest[MINDEX1(length, i1)], UninitCellOf(Any))
      });
      produces #248: _RO(#249, Group(range(0, length, 1), fun(i1: int) {
        ~>(src[MINDEX1(length, i1)], CellOf(Any)(model(i1)))
      }));
      produces #247: Group(range(0, length, 1), fun(i1: int) {
        ~>(dest[MINDEX1(length, i1)], CellOf(Any)(model(i1)))
      });
      __admitted();
      __ignore(memcpy(dest, src, length * sizeof(f64)))
    };
    fun MATRIX2_COPY_double(dest: ptr(f64), src: ptr(f64), n1: int, n2: int): unit [model, #253, #252, #251, #252, #251] {
      requires model: pure_fun(fun(#254: int, #255: int): Type { f64 });
      requires #253: _Fraction;
      consumes #252: _RO(#253, Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          ~>(src[MINDEX2(n1, n2, i1, i2)], CellOf(Any)(model(i1, i2)))
        })
      }));
      consumes #251: Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          ~>(dest[MINDEX2(n1, n2, i1, i2)], UninitCellOf(Any))
        })
      });
      produces #252: _RO(#253, Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          ~>(src[MINDEX2(n1, n2, i1, i2)], CellOf(Any)(model(i1, i2)))
        })
      }));
      produces #251: Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          ~>(dest[MINDEX2(n1, n2, i1, i2)], CellOf(Any)(model(i1, i2)))
        })
      });
      __admitted();
      __ignore(memcpy(dest, src, n1 * n2 * sizeof(f64)))
    };
    fun MATRIX3_COPY_double(dest: ptr(f64), src: ptr(f64), n1: int, n2: int, n3: int): unit [model, #258, #257, #256, #257, #256] {
      requires model: pure_fun(fun(#259: int, #260: int, #261: int): Type {
        f64
      });
      requires #258: _Fraction;
      consumes #257: _RO(#258, Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(src[MINDEX3(n1, n2, n3, i1, i2, i3)], CellOf(Any)(model(i1, i2, i3)))
          })
        })
      }));
      consumes #256: Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(dest[MINDEX3(n1, n2, n3, i1, i2, i3)], UninitCellOf(Any))
          })
        })
      });
      produces #257: _RO(#258, Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(src[MINDEX3(n1, n2, n3, i1, i2, i3)], CellOf(Any)(model(i1, i2, i3)))
          })
        })
      }));
      produces #256: Group(range(0, n1, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(dest[MINDEX3(n1, n2, n3, i1, i2, i3)], CellOf(Any)(model(i1, i2, i3)))
          })
        })
      });
      __admitted();
      __ignore(memcpy(dest, src, n1 * n2 * n3 * sizeof(f64)))
    };
    fun matrix1_span_shift(): __ghost_ret [T, matrix, n1, a, b, MT, M, #263, #262] {
      requires T: Type;
      requires matrix: ptr(T);
      requires n1: int;
      requires a: int;
      requires b: int;
      requires MT: MemType;
      requires M: pure_fun(fun(#264: int): Type { T });
      consumes #263: Group(range(a, b, 1), fun(i: int) {
        ~>(matrix[MINDEX1(n1, i)], CellOf(MT)(M(i)))
      });
      produces #262: Group(range(0, b - a, 1), fun(i: int) {
        ~>(matrix[a][MINDEX1(b - a, i)], CellOf(MT)(M(i + a)))
      });
      __admitted()
    };
    fun matrix1_span_unshift(): __ghost_ret {
      reverts matrix1_span_shift;
      __admitted()
    };
    fun matrix2_span_shift(): __ghost_ret [T, matrix, n1, n2, a, b, MT, M, #266, #265] {
      requires T: Type;
      requires matrix: ptr(T);
      requires n1: int;
      requires n2: int;
      requires a: int;
      requires b: int;
      requires MT: MemType;
      requires M: pure_fun(fun(#267: int, #268: int): Type { T });
      consumes #266: Group(range(a, b, 1), fun(i: int) {
        Group(range(0, n2, 1), fun(j: int) {
          ~>(matrix[MINDEX2(n1, n2, i, j)], CellOf(MT)(M(i, j)))
        })
      });
      produces #265: Group(range(0, b - a, 1), fun(i: int) {
        Group(range(0, n2, 1), fun(j: int) {
          ~>(matrix[a * n2][MINDEX2(b - a, n2, i, j)], CellOf(MT)(M(i + a, j)))
        })
      });
      __admitted()
    };
    fun matrix2_span_unshift(): __ghost_ret {
      reverts matrix2_span_shift;
      __admitted()
    };
    fun matrix3_span_shift(): __ghost_ret [T, matrix, n1, n2, n3, a, b, MT, M, #270, #269] {
      requires T: Type;
      requires matrix: ptr(T);
      requires n1: int;
      requires n2: int;
      requires n3: int;
      requires a: int;
      requires b: int;
      requires MT: MemType;
      requires M: pure_fun(fun(#271: int, #272: int, #273: int): Type { T });
      consumes #270: Group(range(a, b, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(matrix[MINDEX3(n1, n2, n3, i1, i2, i3)], CellOf(MT)(M(i1, i2, i3)))
          })
        })
      });
      produces #269: Group(range(0, b - a, 1), fun(i1: int) {
        Group(range(0, n2, 1), fun(i2: int) {
          Group(range(0, n3, 1), fun(i3: int) {
            ~>(matrix[a * n2 * n3][MINDEX3(b - a, n2, n3, i1, i2, i3)], CellOf(MT)(M(i1 + a, i2, i3)))
          })
        })
      });
      __admitted()
    };
    fun matrix3_span_unshift(): __ghost_ret {
      reverts matrix3_span_shift;
      __admitted()
    };
    ghost(assert_inhabited()[x := arbitrary(pure_fun(fun(#297: int, #298: int, #299: pure_fun(fun(#296: int): Type {
      int
    })): Type { int }))][reduce_int_sum : x]);
    ghost(assert_prop()[proof := admit(pure_fun(fun(n: int, f: pure_fun(fun(#300: int): Type {
      int
    })): Type { __is_true(0 = reduce_int_sum(n, n, f)) }))][reduce_int_sum_empty : proof]);
    ghost(assert_prop()[proof := admit(pure_fun(fun(a: int, b: int, f: pure_fun(fun(#301: int): Type {
      int
    }), #302: __is_true(b >= a), bp1: int, #303: __is_true(bp1 = b + 1)): Type {
      __is_true(reduce_int_sum(a, b, f) + f(b) = reduce_int_sum(a, bp1, f))
    }))][reduce_int_sum_add_right : proof]);
    ghost(assert_prop()[proof := admit(pure_fun(fun(a: int, b: int, f: pure_fun(fun(#304: int): Type {
      int
    }), #305: __is_true(b > a), ap1: int, #306: __is_true(ap1 = a + 1)): Type {
      __is_true(reduce_int_sum(a, b, f) - f(a) = reduce_int_sum(ap1, b, f))
    }))][reduce_int_sum_sub_left : proof]);
    ghost(assert_prop()[proof := admit(pure_fun(fun(a: int, b: int, ap1: int, bp1: int, f: pure_fun(fun(#307: int): Type {
      int
    }), #308: __is_true(b >= a), #309: __is_true(ap1 = a + 1), #310: __is_true(bp1 = b + 1)): Type {
      __is_true(reduce_int_sum(a, b, f) + (f(b) - f(a)) = reduce_int_sum(ap1, bp1, f))
    }))][reduce_int_sum_slide : proof])
  };
  fun one_fork(): unit {
    letmut x = 0;
    let fork_out: __ghost_fn = __ghost_begin(ghost(ro_fork_group()[H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)]));
    for<seq, up>(i = 0, 5, 1) [#275, #274, #274] {
      strict;
      requires #275: _Fraction;
      xconsumes #274: _RO(#275, ~>(x, CellOf(Any)(0)));
      xproduces #274: _RO(#275, ~>(x, CellOf(Any)(0)));
      ghost(ro_split2()[f := #_1, H := ~>(x, CellOf(Any)(0))]);
      ghost(ro_fork_group()[f := #_1 / 2, H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)]);
      ghost(ro_allow_join2()[f := #_1, H := ~>(x, CellOf(Any)(0))]);
      for<seq, up>(j = 0, 5, 1) [#277, #276, #276] {
        strict;
        requires #277: _Fraction;
        xconsumes #276: _RO(#277, ~>(x, CellOf(Any)(0)));
        xproduces #276: _RO(#277, ~>(x, CellOf(Any)(0)));
        __ignore(x + 1)
      };
      ghost(ro_join_group()[H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)])
    };
    __ghost_end(fork_out)
  };
  fun two_forks(): unit {
    letmut x = 0;
    let fork_out: __ghost_fn = __ghost_begin(ghost(ro_fork_group()[H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)]));
    for<seq, up>(i = 0, 5, 1) [#279, #278, #278] {
      strict;
      requires #279: _Fraction;
      xconsumes #278: _RO(#279, ~>(x, CellOf(Any)(0)));
      xproduces #278: _RO(#279, ~>(x, CellOf(Any)(0)));
      ghost(ro_split3()[f := #_1, H := ~>(x, CellOf(Any)(0))]);
      ghost(ro_fork_group()[f := #_1 / 3, H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)]);
      ghost(ro_fork_group()[f := #_1 / 3, H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)]);
      ghost(ro_allow_join3()[f := #_1, H := ~>(x, CellOf(Any)(0))]);
      for<seq, up>(j = 0, 5, 1) [#281, #280, #280] {
        strict;
        requires #281: _Fraction;
        xconsumes #280: _RO(#281, ~>(x, CellOf(Any)(0)));
        xproduces #280: _RO(#281, ~>(x, CellOf(Any)(0)));
        __ignore(x + 1)
      };
      ghost(ro_join_group()[H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)]);
      ghost(ro_join_group()[H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)])
    };
    __ghost_end(fork_out)
  };
  fun two_forks_spe_twice(): unit {
    letmut x = 0;
    let fork_out: __ghost_fn = __ghost_begin(ghost(ro_fork_group()[H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)]));
    for<seq, up>(i = 0, 5, 1) [#283, #282, #282] {
      strict;
      requires #283: _Fraction;
      xconsumes #282: _RO(#283, ~>(x, CellOf(Any)(0)));
      xproduces #282: _RO(#283, ~>(x, CellOf(Any)(0)));
      ghost(ro_split2()[f := #_1, H := ~>(x, CellOf(Any)(0))]);
      ghost(ro_split2()[f := #_1 / 2, H := ~>(x, CellOf(Any)(0))]);
      ghost(ro_fork_group()[f := #_1 / 2, H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)]);
      ghost(ro_fork_group()[f := #_1 / 2 / 2, H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)]);
      ghost(ro_allow_join2()[f := #_1 / 2, H := ~>(x, CellOf(Any)(0))]);
      for<seq, up>(j = 0, 5, 1) [#285, #284, #284] {
        strict;
        requires #285: _Fraction;
        xconsumes #284: _RO(#285, ~>(x, CellOf(Any)(0)));
        xproduces #284: _RO(#285, ~>(x, CellOf(Any)(0)));
        __ignore(x + 1)
      };
      ghost(ro_join_group()[H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)]);
      ghost(ro_allow_join2()[f := #_1, H := ~>(x, CellOf(Any)(0))]);
      ghost(ro_join_group()[H := ~>(x, CellOf(Any)(0)), r := range(0, 5, 1)])
    };
    __ghost_end(fork_out)
  }
}

